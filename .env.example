
# Supabase (Auth + Database + Storage)

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Backend
# Direct Postgres connection (from Supabase → Settings → Database → Connection string)

DATABASE_URL=postgresql+asyncpg://postgres.ref:password@aws-0-region.pooler.supabase.com:6543/postgres

# App secret for any internal signing (e.g., CSRF tokens)

APP_SECRET_KEY=change-me-to-a-random-string

# LemonSqueezy (Billing) — set up later when you have a deployed URL

LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_STORE_ID=
LEMONSQUEEZY_VARIANT_ID=

# Cloudflare R2 (Object Storage)

R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=tenaciti-uploads

# AI (Groq LLM)

LLM_API_KEY=your-groq-api-key
LLM_PROVIDER=groq

# Tier Limits

FREE_UPLOAD_LIMIT=3
PRO_UPLOAD_LIMIT=20
FREE_MAX_FILE_SIZE_MB=10
PRO_MAX_FILE_SIZE_MB=25

# CORS (comma-separated origins)

CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend (used by Vite — prefixed with VITE_)

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_API_URL=http://localhost:8000/api/v1

# Email (Resend)
# Get API key from https://resend.com/api-keys

RESEND_API_KEY=re_your_resend_api_key
# Use onboarding@resend.dev for testing (no domain needed)
# Replace with noreply@yourdomain.com after verifying a domain in Resend
MAIL_FROM=onboarding@resend.dev
